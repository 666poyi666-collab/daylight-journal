# 拾光项目 AI 协作规范

你正在修改「拾光 · 日记复盘」。开始工作前先读取：

1. `README.md`
2. `docs/README.md`
3. `docs/REQUIREMENTS.md`
4. `docs/BUGS.md`
5. `docs/ITERATION-LOG.md` 的最近一次记录
6. 与任务相关的专题文档

## 项目事实

- 技术栈：React 19 + TypeScript + Vite + Capacitor Android
- 默认数据策略：本地 localStorage + MCP 服务端 JSON 副本
- AI 主链路：ChatGPT 网页端「日记」项目 + 「拾光日记」MCP 连接器
- 第一阶段不使用 OpenAI API，不接入 Time One/滴答清单，不做 Obsidian 双向写入

## 开发规则

- 先说明要解决的根因、影响范围和验收方式，再修改代码。
- 开工前先确认关联的 `FR-xxx` 或 `NFR-xxx`；新需求必须先登记编号和验收条件再实现。
- 发现 Bug 或工程风险时立即登记 `BUG-xxx` 或 `KR-xxx`，修复后补根因和回归证据。
- 优先最小改动；不要为了局部问题重写整个页面或引入新的运行时。
- 读取现有实现后再抽象，避免重复造轮子。
- 业务逻辑、同步协议和视觉决策分别更新对应文档。
- 不把真实日记、令牌、Cookie、浏览器会话或完整日志提交到仓库。
- 修改同步/MCP 后必须运行构建并验证接口；修改 Android 后必须重新安装并检查目标设备。

## 每次迭代必须落账

- 在 `docs/ITERATION-LOG.md` 新增唯一的 `ITER-YYYYMMDD-NN` 记录，逐项列出新增、修改、删除的文件和行为，不能只写“优化”。
- 在同一记录中写清关联需求、根因、影响范围、验收方式、实际验证结果和遗留项；没有删除或没有新 Bug 时明确写“无”。
- 同步更新 `docs/REQUIREMENTS.md` 的需求状态、统计与分析；不得用聊天记录代替需求事实。
- 同步更新 `docs/BUGS.md`；修复过的问题必须留下根因和回归证据，防止同类问题复发。
- 用户可感知的变化更新 `docs/CHANGELOG.md`；纯过程记录只写迭代日志，避免重复堆砌。
- 提交前执行 `npm run check:governance`，确保编号、台账结构和文档入口有效。

## 代码风格

- TypeScript 使用单引号、无分号、明确类型；遵循现有 ESLint/Oxlint 配置。
- React 组件保持单一职责；复杂逻辑抽成命名清晰的函数或模块。
- 注释解释“为什么”，不重复“代码正在做什么”。
- 重要公共函数使用简短 JSDoc，说明输入、输出、失败行为。
- UI 文案使用中文，变量、函数、接口名称使用英文。

## 调试输出

遇到问题先记录：用户动作、预期、实际、复现步骤、端、请求 URL、状态码、关键状态变化。日志必须脱敏且可删除，不在生产 UI 中输出堆栈。

## 完成标准

- `npm run check:governance` 成功
- `npm run build` 成功
- 相关接口和端到端链路验证通过
- 离线或失败时不丢稿
- 需求、操作日志、Bug 台账和变更说明已按本次影响同步更新
